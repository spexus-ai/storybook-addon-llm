export const ADDON_ID = 'storybook-addon-llm';
export const PANEL_ID = `${ADDON_ID}/panel`;
export const TOOL_ID = `${ADDON_ID}/tool`;
export const KEY = `${ADDON_ID}/pick`;

export const EVENTS = {
  ELEMENT_SELECTED: `${ADDON_ID}/element-selected`,
  PICK_CANCELLED: `${ADDON_ID}/pick-cancelled`,
  PICK_ERROR: `${ADDON_ID}/pick-error`,
  APPLY_STYLES: `${ADDON_ID}/apply-styles`,
  RESET_STYLES: `${ADDON_ID}/reset-styles`,
};
